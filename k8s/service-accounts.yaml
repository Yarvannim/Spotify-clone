apiVersion: v1
kind: ServiceAccount
metadata:
  name: keycloak-sa
  namespace: spotify-clone
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::${AWS_ACCOUNT_ID}:role/spotify-clone-keycloak-service-role

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: stream-service-sa
  namespace: spotify-clone
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::${AWS_ACCOUNT_ID}:role/spotify-clone-stream-service-role

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: frontend-sa
  namespace: spotify-clone
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::${AWS_ACCOUNT_ID}:role/spotify-clone-frontend-service-role